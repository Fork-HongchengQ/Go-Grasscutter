# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main", "actions" ]
  pull_request:
    branches: [ "main", "actions" ]

jobs:

  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22'

    - name: Build
      run: go build Go-Grasscutter

    - name: Upload build
      uses: actions/upload-artifact@v4
      with:
        name: Go-Grasscutter_ubuntu
        path: Go-Grasscutter

  build-win:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22'

    - name: Build
      run: go build Go-Grasscutter

    - name: Upload build
      uses: actions/upload-artifact@v4
      with:
        name: Go-Grasscutter_win
        path: Go-Grasscutter.exe